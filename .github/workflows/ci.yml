name: CI

on:
  pull_request:
  push:
    branches:
      - "feature/**"

jobs:
  rustfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: v0-rust
          add-job-id-key: false
          shared-key: v0-rust-feature
      - name: Check formatting
        run: cargo fmt --all -- --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: v0-rust
          add-job-id-key: false
          shared-key: v0-rust-feature
      - name: Lint with clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            floe_core:
              - "crates/floe-core/**"
            floe_cli:
              - "crates/floe-cli/**"
            core_io:
              - "crates/floe-core/src/io/**"
              - "crates/floe-core/tests/io/**"
              - "crates/floe-core/tests/io_*.rs"
            core_run:
              - "crates/floe-core/src/run/**"
              - "crates/floe-core/tests/run/**"
              - "crates/floe-core/tests/run_*.rs"
            core_config:
              - "crates/floe-core/src/config/**"
              - "crates/floe-core/tests/config/**"
              - "crates/floe-core/tests/config.rs"
            docs:
              - "docs/**"
              - "README.md"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: v0-rust
          add-job-id-key: false
          shared-key: v0-rust-feature
      - name: Run tests
        env:
          CARGO_BUILD_JOBS: "1"
          RUST_TEST_THREADS: "1"
          RUSTFLAGS: "-C debuginfo=0"
        run: |
          if [ "${{ steps.changes.outputs.floe_core }}" = "true" ]; then
            if [ "${{ steps.changes.outputs.core_config }}" = "true" ]; then
              cargo test -p floe-core --test config
            fi
            if [ "${{ steps.changes.outputs.core_io }}" = "true" ]; then
              cargo test -p floe-core --test io_format --test io_storage --test io_read --test io_write
            fi
            if [ "${{ steps.changes.outputs.core_run }}" = "true" ]; then
              cargo test -p floe-core --test run_checks --test run_normalize --test run_report --test run_schema_mismatch --test run_check_order --test run_entity
            fi
            cargo test -p floe-core --doc
          fi
          if [ "${{ steps.changes.outputs.floe_cli }}" = "true" ]; then
            cargo test -p floe-cli --tests
          fi
