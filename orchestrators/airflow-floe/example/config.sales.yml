version: "0.2"
metadata:
  project: "floe-airflow-example-sales"

domains:
  - name: "sales"
    incoming_dir: "./in/sales"

report:
  path: "./report"

entities:
  - name: "orders"
    domain: "sales"
    source:
      format: "csv"
      path: "./in/sales/orders.csv"
      options:
        header: true
        separator: ","
    sink:
      accepted:
        format: "delta"
        path: "./out/accepted/orders_delta"
      rejected:
        format: "csv"
        path: "./out/rejected/orders"
    policy:
      severity: "reject"
    schema:
      columns:
        - name: "order_id"
          type: "string"
          nullable: false
          unique: true
        - name: "created_at"
          type: "datetime"
          nullable: false
        - name: "amount"
          type: "number"
          nullable: false
